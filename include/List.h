/*#ifndef LIST_H
#define LIST_H

template <typename type>
class Node
{
    type value;
    Node * next;
    Node * prev;
};

template <typename type>
class List
{
    private:
        Node <type> * head;
        Node <type> * tail;
    public:
        List();
        List(int value, int initial_size);
        virtual ~List();
};

#endif // LIST_H
*/
